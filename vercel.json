{
  "version": 2,
  "builds": [
    {
      "src": "server.ts",
      "use": "@now/node"
    }
  ],
  "routes": [
    {
      "src": "/api/users(.*)",
      "dest": "userRoutes.ts"
    },
    {
      "src": "/(.*)",
      "dest": "server.ts"
    }
  ]
}
